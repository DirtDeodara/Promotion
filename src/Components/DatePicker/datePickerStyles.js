import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  picker: {
    width: 200,
    marginBottom: 10,
    borderColor: "black",
    borderWidth: 3.5,
    borderRadius: 10,
    backgroundColor: 'white'
  }
});
