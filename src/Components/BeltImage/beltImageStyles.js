import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    justifyContent: "flex-end",
    height: 30,
    marginTop: 8,
    marginBottom: 10,
    borderColor: "black",
    borderWidth: 1
  }
})